{% extends '/admin/index.html.twig' %}

{% block title %}AnimalPicture{% endblock %}

{% block content %}
    <h1>AnimalPicture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal_picture.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ animal_picture.name }}</td>
            </tr>
            <tr>
                <th>DescriptionAlt</th>
                <td>{{ animal_picture.descriptionAlt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_animal_picture_index') }}">back to list</a>

    <a href="{{ path('app_animal_picture_edit', {'id': animal_picture.id}) }}">edit</a>

    {{ include('animal_picture/_delete_form.html.twig') }}
{% endblock %}
