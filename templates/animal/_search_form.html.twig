{{ form_start(form) }}
    {{ form_row(form.q) }}

    <h4>Race</h4>
    {{ form_row(form.race) }}

    <h4>Habitat</h4>
    {{ form_row(form.habitat) }}

    <button type='submit' class='btn btn-primary w-100'>Filtrer</button>

{{ form_end(form) }}