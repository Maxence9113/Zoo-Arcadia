{% extends '/admin/index.html.twig' %}

{% block title %}Meal
{% endblock %}

{% block content %}
	<h1>Meal</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ meal.id }}</td>
			</tr>
			<tr>
				<th>Quantity</th>
				<td>{{ meal.quantity }}</td>
			</tr>
			<tr>
				<th>Date</th>
				<td>{{ meal.date ? meal.createdAt|date('Y-m-d-H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_meal_index') }}">back to list</a>

	<a href="{{ path('app_meal_edit', {'id': meal.id}) }}">edit</a>

	{{ include('meal/_delete_form.html.twig') }}
{% endblock %}
